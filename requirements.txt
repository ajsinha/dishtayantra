# DishtaYantra Compute Server v2.0.0 - Python dependencies
# ==========================================================
# Core web stack (v2.0.0: Flask replaced by FastAPI + uvicorn)
fastapi>=0.110
uvicorn[standard]>=0.29
jinja2>=3.1
itsdangerous>=2.1
python-multipart>=0.0.9

# Database layer (users / roles / API keys)
SQLAlchemy>=2.0

# Messaging / brokers
kafka-python==2.2.15
confluent-kafka==2.6.1
stomp.py==8.1.0

# SQL pub/sub drivers
pymysql==1.1.2
psycopg2-binary==2.9.11

# Other pub/subs and caches
aerospike==18.0.0
redis==6.4.0

# High availability (zookeeper provider)
kazoo==2.10.0

# Data processing
polars==1.34.0
pandas==2.3.3
pyarrow==22.0.0

# Native integrations
py4j==0.10.9.7
lmdb==1.5.1

# Monitoring / utilities
psutil==6.1.0
prometheus-client>=0.20
requests==2.32.3
msgpack==1.1.0
markdown==3.7
Pygments==2.18.0

# Configuration (v2.2: YAML is the recommended config format)
PyYAML>=6.0

# Testing
pytest>=8.0
httpx>=0.27

# --------------------------------------------------------------------------
# Optional cloud providers - install only what you configure:
#   storage.provider=s3 / s3:// pub-sub / ha.provider=s3   -> boto3
#   sqs:// / kinesis:// / sns:// pub-sub                    -> boto3
#   storage.provider=azureblob / azureblob:// pub-sub      -> azure-storage-blob
#   servicebus:// pub-sub                                  -> azure-servicebus
#   eventhubs:// pub-sub                                   -> azure-eventhub
#   storage.provider=gcs / gcs:// pub-sub                  -> google-cloud-storage
# --------------------------------------------------------------------------
# boto3>=1.34
# azure-storage-blob>=12.19
# azure-servicebus>=7.12
# azure-eventhub>=5.11
# google-cloud-storage>=2.16

# --------------------------------------------------------------------------
# Optional at-rest encryption for trusted-server API keys (service-plane
# split). Required ONLY if you register trusted servers in the UI; the base
# install stays dependency-light. See 'Service-plane split' in Help.
# --------------------------------------------------------------------------
# cryptography>=42.0

# --------------------------------------------------------------------------
# Optional WebAssembly calculator runtime - install only if you use
# WasmCalculator (core.calculator.wasm_calculator). Bundles the engine; no
# host C/Rust compiler needed. See the "WebAssembly (WASM) Sandboxed
# Calculators Guide" in Help.
# --------------------------------------------------------------------------
# wasmtime>=20.0

# --------------------------------------------------------------------------
# Front-end assets (Bootstrap, jQuery, Prism, highlight.js, fonts) are
# VENDORED under web/static/vendor/ - no CDN or npm install is needed at
# runtime, so the UI renders fully in air-gapped deployments.
# --------------------------------------------------------------------------
